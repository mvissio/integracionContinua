machine:
  node:
    version: 8.11.2
  services:
    - docker
dependencies:
  override:
    - docker build -t platzi .

test:
  override:
    - docker run platzi npm test

deployment:
  qa:
    branch: /feature.*/
    commands:
      - sh deployments/qa.sh